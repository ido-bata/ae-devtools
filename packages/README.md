# packages/

将来的な monorepo 化のために用意されたディレクトリ。

現時点では中身はほぼ空。
この領域の構造はビジョン段階を超えて決定されておらず、
最初の実装フェーズに入った段階で
`vscode-extension`, `mcp-server`, `core`, `shared` のような
候補パッケージが検討される可能性があるが、
命名・責務分割・ビルド構成はまだ未確定。

詳細はリポジトリルートの `docs/vision.md` を参照。